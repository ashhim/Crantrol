rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all reads and writes unless a collection is explicitly needed in the future
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
